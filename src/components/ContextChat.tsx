// Stub — ContextChat foi removido neste fork. Se quiser trazer de volta,
// reimporte de PreceptorMED + edge function ai-chat + tabela ai_chat_messages.
export default function ContextChat(_props: { context?: string; contextLabel?: string; suggestions?: string[] }) {
  return null;
}
